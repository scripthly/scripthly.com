services:
  scripthly:
    image: ghcr.io/scripthly/scripthly.com:${IMAGE_TAG:-latest}
    build: .
    container_name: scripthly
    restart: unless-stopped
    environment:
      LOG_LEVEL: INFO
      FORCE_COLOR: "1"
    ports:
      # ? The port the reverse proxy forwards to.
      - "${HOST_PORT:-4444}:4000"
